% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npcra.R
\name{compute_npcra}
\alias{compute_npcra}
\title{Non-parametric circadian rhythm analysis (NPCRA)}
\usage{
compute_npcra(
  x,
  epoch_s = NULL,
  L5_hours = 5,
  M10_hours = 10,
  window_days = NULL
)
}
\arguments{
\item{x}{A \code{zeitr_recording} as returned by \code{\link[=read_actigraphy]{read_actigraphy()}}, or a
data frame / tibble with at least \code{datetime} and \code{activity} columns.
If a \code{state} column is present, off-wrist epochs (\code{state == 4}) are
excluded before computing all NPCRA variables.}

\item{epoch_s}{\code{numeric(1)}. Epoch duration in seconds. If \code{NULL}
(default), estimated automatically from the median inter-epoch interval.}

\item{L5_hours}{\code{numeric(1)}. Width of the least-active window in hours.
Default is \code{5}.}

\item{M10_hours}{\code{numeric(1)}. Width of the most-active window in hours.
Default is \code{10}.}

\item{window_days}{\code{numeric(1)} or \code{NULL}. If supplied, the recording is
split into non-overlapping windows of this length (in days) and NPCRA
variables are computed for each window. A \code{window_start} column is added
to the output. Partial final windows (shorter than \code{window_days}) are
included but flagged via a lower \code{n_days} value. Default \code{NULL} computes
a single estimate over the full recording.}
}
\value{
A tibble with columns \code{participant_id}, \code{window_start} (if
\code{window_days} is set), \code{IS}, \code{IV}, \code{RA}, \code{L5}, \code{L5_onset}, \code{M10},
\code{M10_onset}, \code{n_days}, \code{n_epochs}.
}
\description{
Computes the standard non-parametric circadian rhythm analysis variables
from an actigraphy recording, following Gonçalves et al. (2014) and Van
Someren et al. (1999). All variables are derived from the 24-hour average
activity profile built from \strong{hourly means} (p = 24).
}
\details{
The following variables are computed:

\describe{
\item{\code{IS}}{\strong{Interdaily stability} — consistency of the 24 h rest-activity
pattern across days (range 0--1; higher = more stable).}
\item{\code{IV}}{\strong{Intradaily variability} — fragmentation of the
rest-activity rhythm (>= 0; higher = more fragmented).}
\item{\code{RA}}{\strong{Relative amplitude} — contrast between the most active
10 h window (M10) and least active 5 h window (L5) (range 0--1).}
\item{\code{L5}}{Mean activity during the least active 5 consecutive hours.}
\item{\code{L5_onset}}{Clock time of the L5 window onset (hh:mm).}
\item{\code{M10}}{Mean activity during the most active 10 consecutive hours.}
\item{\code{M10_onset}}{Clock time of the M10 window onset (hh:mm).}
}
}
\examples{
\dontrun{
rec   <- read_actigraphy("recordings/P001.txt")

# Single estimate over the full recording
compute_npcra(rec)

# Per-fortnight estimates
compute_npcra(rec, window_days = 14)
}
}
\references{
Gonçalves, B. S. B., Adamowicz, T., Louzada, F. M., Moreno, C. R., &
Araujo, J. F. (2014). A fresh look at the use of nonparametric analysis in
actimetry. \emph{Sleep Medicine Reviews}, 20, 84--91.
\doi{10.1016/j.smrv.2014.06.002}

Van Someren, E. J. W., Swaab, D. F., Colenda, C. C., Cohen, W.,
McCall, W. V., & Rosenquist, P. B. (1999). Bright light therapy:
Improved sensitivity to its effects on rest-activity rhythms in
Alzheimer patients by application of nonparametric methods.
\emph{Chronobiology International}, 16(4), 505--518.
\doi{10.3109/07420529908998724}
}
