% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lri.R
\name{compute_lri}
\alias{compute_lri}
\title{Light Regularity Index (LRI)}
\usage{
compute_lri(x, col = "light", threshold = NULL, log_transform = FALSE)
}
\arguments{
\item{x}{A \code{zeitr_recording}/\code{zeitr_result}, or a data frame / tibble
with at least a \code{datetime} column and the column named in \code{col}.}

\item{col}{\code{character(1)}. Name of the light channel column. Default
\code{"light"}.}

\item{threshold}{\code{numeric(1)} or \code{NULL}. Threshold applied to \code{col}
(\code{col > threshold} = "light exposure"). Default \code{NULL}, which computes
all five of the reference notebook's default thresholds (\code{10}, \code{20},
\code{50}, \code{100}, \code{300}, applied as \code{1}, \code{1.301}, \code{1.699}, \code{2}, \code{2.4771} --
see Details on why these numbers don't mean lux against raw data) and
returns one row with columns \code{LRI_10}...\code{LRI_300}. Pass a single
number for one custom threshold instead (returns a single \code{LRI}
column).}

\item{log_transform}{\code{logical(1)}. If \code{TRUE}, applies \code{log10(col + 1)}
before thresholding -- the probably-originally-intended behaviour
implied by the reference notebook's (incorrect, per Details) comment.
Default \code{FALSE}, matching what the reference actually computes.}
}
\value{
A tibble with \code{participant_id}, either \code{LRI_10}, \code{LRI_20},
\code{LRI_50}, \code{LRI_100}, \code{LRI_300} (default, \code{threshold = NULL}) or a
single \code{LRI} column (custom \code{threshold}), and \code{n_epochs}.
}
\description{
Computes the Light Regularity Index (Hand et al. 2023): a measure of
day-to-day consistency in light exposure timing, analogous to the Sleep
Regularity Index but applied to a binarized light-exposure signal.
}
\details{
Ports the \code{artvalencio/pyActigraphy} fork's actual \code{LRI()} method
(\code{pyActigraphy/light/light_metrics.py}) exactly -- found there after an
earlier investigation confirmed it does not exist anywhere in the
official \code{ghammad/pyActigraphy} repository (any branch, any commit in
history). \code{LRI()}'s own inner \code{prob_stability()}/\code{lri_profile()}
functions are structurally identical to \code{compute_sri()}'s \code{"sadeh"}/
\code{"ck"}/\code{"scripps"}/\code{"roenneberg"} paths' aggregation
(\code{.sri_pyactigraphy()}) -- verified by direct execution that \code{LRI()}'s
own code and a direct call to \code{sri()} (\code{pyActigraphy/sleep/scoring/ sri.py}) give IDENTICAL results on the same real light data, to full
floating-point precision. \code{compute_lri()} reuses \code{.sri_pyactigraphy()}
directly rather than duplicating the same formula under a different
name.

\strong{A real discrepancy worth knowing about, not a guess}: the
reference notebook's own comment (Cell 16 of
\code{vs_condor_py_pipeline_fix30_jrsv.ipynb}) claims "pyActigraphy stores
light data in log10-transformed lux internally," and picks its default
thresholds accordingly (\verb{10/20/50/100/300 lux -> log10 -> 1.0/1.301/1.699/2.0/2.4771}). This is not what the code actually does:
read directly, \code{RawATR}'s constructor
(\code{pyActigraphy/io/atr/atr.py}) passes the \code{LIGHT} column straight into
\code{LightRecording} with no \code{log10()} call anywhere, and real light values
from actual ActTrust recordings confirm this empirically -- raw \code{LIGHT}
ranges from 0 to over 20,000 in real data checked this session, nowhere
near the ~0-5 range \code{log10(lux)} would produce. So the reference
Python's own \code{LRI_10}/\code{LRI_20}/etc. columns almost certainly don't
measure what their names claim -- thresholds of ~1-2.5 applied to raw,
untransformed light amount to "any detectable light vs. darkness," not
"50 lux vs. below." This function replicates the reference's actual
behaviour (raw threshold, no transform) for parity purposes -- matching
what the real pipeline computes, not what its comments say it computes.
\code{log_transform = TRUE} is available if you want the -- probably
originally intended -- log10(lux) behaviour instead; it is NOT the
default, since the default should match the real reference output.
}
\examples{
\dontrun{
result <- run_pipeline_native("recordings/P001.txt", tz = "America/Sao_Paulo")
compute_lri(result)
compute_lri(result, threshold = 50)
}
}
\references{
Hand, A. J., Lane, J. M., Payne, A. C., ... & Barker, D. H. (2023).
Measuring light regularity: sleep regularity is associated with
regularity of light exposure in adolescents. \emph{Sleep}, 46(8), zsad001.
\doi{10.1093/sleep/zsad001}
}
\seealso{
\code{\link[=compute_sri]{compute_sri()}}, \code{\link[=compute_cosinor]{compute_cosinor()}}
}
