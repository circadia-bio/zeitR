% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study_sleep_metrics.R
\name{study_sleep_metrics}
\alias{study_sleep_metrics}
\title{Batch sleep-timing and chronotype metrics across a study}
\usage{
study_sleep_metrics(
  results,
  min_tib_h = 5,
  min_tib_eve_h = 3,
  tz = "UTC",
  holidays = NULL,
  free_days = NULL
)
}
\arguments{
\item{results}{A named list of \code{zeitr_result} objects, as returned by
\code{\link[=run_pipeline_batch]{run_pipeline_batch()}} or \code{\link[=run_pipeline_native_batch]{run_pipeline_native_batch()}}. \code{participant_id}
is taken from each result's own \verb{$subject_id}, falling back to the list
name if that is unavailable.}

\item{min_tib_h}{\code{numeric(1)}. Minimum total in-bed time (hours) for a
night to be included in \code{\link[=compute_sleep_metrics]{compute_sleep_metrics()}}. Default \code{5.0}.}

\item{min_tib_eve_h}{\code{numeric(1)}. Minimum TBT (hours) for a night to
qualify as a free-day-eve night in \code{\link[=compute_cpd_metrics]{compute_cpd_metrics()}}. Default \code{3.0}.}

\item{tz}{\code{character(1)}. Time zone for extracting clock hours. Default
\code{"UTC"}.}

\item{holidays, free_days}{Forwarded to both \code{\link[=compute_sleep_metrics]{compute_sleep_metrics()}} and
\code{\link[=compute_cpd_metrics]{compute_cpd_metrics()}} for every participant. Default \code{NULL} for both,
in which case each participant's own \code{result$holidays}/\code{result$free_days}
(set when the pipeline was run) are used instead. Supplying either here
overrides that per-participant default for the whole study.}
}
\value{
A tibble with one row per participant: \code{participant_id}, all
\code{\link[=compute_sleep_metrics]{compute_sleep_metrics()}} columns (\code{n_overall}/\code{n_wd}/\code{n_fd} and the
twelve sleep-timing metrics with \verb{_wd}/\verb{_fd} suffixes), and all
\code{\link[=compute_cpd_metrics]{compute_cpd_metrics()}} columns (\code{n_nights_cpd}, \code{n_free_days},
\code{n_workdays}, \code{msw_h}/\code{msf_h}/\code{msfsc_h} and their \verb{_hms} forms, \code{sjl_h},
\code{sjla_h}, \code{cpd_s}/\code{cpd_min}/\code{cpd_h}).
}
\description{
Computes \code{\link[=compute_sleep_metrics]{compute_sleep_metrics()}} and \code{\link[=compute_cpd_metrics]{compute_cpd_metrics()}} for every
participant in a batch of pipeline results and stacks them into a single
tibble with one row per participant -- the sleep-timing/chronotype
counterpart to \code{\link[=study_summary]{study_summary()}}, which covers NPCRA activity-rhythm
variables instead.
}
\details{
\code{\link[=compute_sleep_metrics]{compute_sleep_metrics()}} and \code{\link[=compute_cpd_metrics]{compute_cpd_metrics()}} each return a single
named list per participant with no participant identifier, and there was
previously no batch wrapper analogous to \code{\link[=study_summary]{study_summary()}} for them. This
is that wrapper -- intended to make these chronobiological phenotyping
metrics database-ready for tools like \code{syncR::sync()}, which expect one
row per participant with a shared \code{participant_id} column across sources.

If either metric computation fails for a participant (e.g. no free days
found, or no nights pass the \code{min_tib_h} filter), that participant's row
is filled with \code{NA} for the affected metrics and a warning is emitted --
the rest of the study is unaffected.
}
\examples{
\dontrun{
results <- run_pipeline_native_batch("recordings/", tz = "America/Sao_Paulo")
study_sleep_metrics(results)

# Feed straight into syncR::sync()
sync(zeit = study_sleep_metrics(results))
}
}
\seealso{
\code{\link[=study_summary]{study_summary()}} for the NPCRA (activity-rhythm) analogue,
\code{\link[=compute_sleep_metrics]{compute_sleep_metrics()}}, \code{\link[=compute_cpd_metrics]{compute_cpd_metrics()}},
\code{\link[=run_pipeline_native_batch]{run_pipeline_native_batch()}}
}
