% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sleep_classify.R
\name{classify_sleep_episodes}
\alias{classify_sleep_episodes}
\title{Classify sleep episodes as main or secondary (native pipeline)}
\usage{
classify_sleep_episodes(
  episodes,
  data,
  max_tib_h = 16,
  max_main_tib_h = 14,
  min_main_tib_h = 4,
  nocturnal_onset_start = 18,
  nocturnal_onset_end = 6,
  temp_thresh = 28,
  light_thresh_window = 5,
  light_thresh_recovery = 10,
  min_fragment_tib_h = 1,
  rolling_window_min = 15L,
  max_split_iterations = 3L,
  collision_gap_h = 4,
  verbose = FALSE
)
}
\arguments{
\item{episodes}{A tibble as returned by \code{\link[=extract_sleep_episodes]{extract_sleep_episodes()}}.}

\item{data}{A tibble as returned by \code{\link[=detect_sleep_crespo]{detect_sleep_crespo()}}, containing
at minimum \code{datetime}, \code{state}, \code{activity}. The ActTrust channels
\code{int_temp} (wrist temperature) and \code{light} (ambient lux) must be
present for Fix 26a/c; the function falls back to defaults when absent.}

\item{max_tib_h}{\code{numeric(1)}. Episodes with TBT > this value are excluded
directly. Default \code{16}.}

\item{max_main_tib_h}{\code{numeric(1)}. Episodes with TBT in
\verb{(max_main_tib_h, max_tib_h]} are split first. Default \code{14}.}

\item{min_main_tib_h}{\code{numeric(1)}. Minimum TBT (hours) for a main
episode. Default \code{4}.}

\item{nocturnal_onset_start}{\code{numeric(1)}. Default nocturnal window start
(decimal hours). Overridden by Fix 26a. Default \code{18}.}

\item{nocturnal_onset_end}{\code{numeric(1)}. Default nocturnal window end.
Overridden by Fix 26a. Default \code{6}.}

\item{temp_thresh}{\code{numeric(1)}. Minimum wrist temperature (degC) for
candidate episodes in Fix 26a/c. Default \code{28}.}

\item{light_thresh_window}{\code{numeric(1)}. Maximum ambient light (lux) for
the nocturnal window inference (Fix 26a). Default \code{5}.}

\item{light_thresh_recovery}{\code{numeric(1)}. Maximum ambient light (lux) for
gap merging in fragment recovery (Fix 26c). Default \code{10}.}

\item{min_fragment_tib_h}{\code{numeric(1)}. Minimum TBT (hours) for each
fragment produced by an episode split (Fix 29). Default \code{1}.}

\item{rolling_window_min}{\code{integer(1)}. Smoothing window (epochs) for the
activity signal used to locate episode split points. Default \code{15L}.}

\item{max_split_iterations}{\code{integer(1)}. Maximum recursive splits per
episode. Default \code{3L}.}

\item{collision_gap_h}{\code{numeric(1)}. Minimum gap (hours) between two
same-date main episodes to trigger sleep-date reassignment (Fix 26b).
Default \code{4}.}

\item{verbose}{\code{logical(1)}. Print step-by-step diagnostics. Default
\code{FALSE}.}
}
\value{
A tibble with the same columns as \code{episodes} plus \code{sleep_type}
(\code{"main"} or \code{"secondary"}) and \code{is_nap} (logical, \code{TRUE} when
\code{sleep_type == "secondary"}, for backwards compatibility with the
\code{zeitr_result$nights} schema).
}
\description{
Applies the JRSV classification rule set to a raw episode table from
\code{\link[=extract_sleep_episodes]{extract_sleep_episodes()}}, assigning each episode a \code{sleep_type} of
\code{"main"} or \code{"secondary"}. The execution order follows Fix 27:
}
\details{
\enumerate{
\item \strong{Fix 25} -- exclude truncated episodes at the recording end.
\item \strong{Fix 26a} -- infer adaptive nocturnal window from \code{int_temp}
and \code{light}.
\item \strong{Fix 29} -- split TBT 14-16 h episodes first; exclude TBT > 16 h
directly without attempting a split.
\item \strong{Fix 26c} -- recover fragmented sleep nights missed by the scorer.
\item \strong{Rules 3-5} -- classify as main or secondary using the nocturnal
window and \code{min_main_tib_h}.
\item \strong{Fix 26b} -- resolve sleep-date collisions from the noon threshold.
\item \strong{Rule 6} -- keep the longest main episode per sleep date.
\item \strong{Rule 7} -- exclude all episodes on days with no main sleep.
}
}
\seealso{
\code{\link[=extract_sleep_episodes]{extract_sleep_episodes()}}, \code{\link[=run_pipeline_native]{run_pipeline_native()}}
}
