% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circ_utils.R
\name{circ_sd_h}
\alias{circ_sd_h}
\title{Circular standard deviation of clock times in decimal hours}
\usage{
circ_sd_h(x)
}
\arguments{
\item{x}{\code{numeric}. Decimal hours in [0, 24). \code{NA} values are silently
dropped. Returns \code{NA_real_} for fewer than 2 values.}
}
\value{
A single non-negative \code{numeric} (hours), or \code{NA_real_}.
}
\description{
Computes the circular SD using the mean resultant length formula:
\code{sqrt(-2 * log(R_bar)) * 24 / (2*pi)}, where \code{R_bar} is the mean
resultant length of the unit-circle representation of the times.
Linear SD inflates when times straddle midnight; this measure is
invariant to the wrap point (Fix 24 in the JRSV pipeline).
}
\examples{
circ_sd_h(c(23.5, 0.5))   # small SD for times close to midnight
circ_sd_h(c(6.0, 18.0))   # large SD for times 12 h apart
}
\references{
Mardia, K. V., & Jupp, P. E. (2000). \emph{Directional Statistics} (2nd ed.).
Wiley.
}
