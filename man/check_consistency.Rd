% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consistency.R
\name{check_consistency}
\alias{check_consistency}
\title{Check actigraphy timestamps for consistency issues}
\usage{
check_consistency(x, gap_s = 120, datetime_col = "datetime")
}
\arguments{
\item{x}{A tibble as returned by \code{\link[=read_acttrust]{read_acttrust()}} or
\code{\link[=prepare_actigraphy]{prepare_actigraphy()}}, containing a \code{datetime} column.}

\item{gap_s}{\code{numeric(1)}. Gap threshold in seconds. Intervals longer than
this are flagged. Default is \code{120} (2 minutes).}

\item{datetime_col}{\code{character(1)}. Name of the datetime column.
Default is \code{"datetime"}.}
}
\value{
A tibble with one row per detected issue and columns:
\describe{
\item{\code{row}}{\code{integer} — row index in \code{x} where the issue occurs.}
\item{\code{datetime}}{\code{POSIXct} — timestamp at that row.}
\item{\code{issue}}{\code{character} — one of \code{"gap"}, \code{"backward_jump"}, or
\code{"year_artefact"}.}
\item{\code{detail}}{\code{character} — human-readable description.}
}
Returns a zero-row tibble if no issues are found.
}
\description{
Scans a recording tibble for three classes of timestamp problem:
}
\details{
\itemize{
\item \strong{Gaps} — intervals between consecutive epochs longer than \code{gap_s}
seconds.
\item \strong{Backward jumps} — timestamps that go backwards in time.
\item \strong{Year artefacts} — timestamps in the years 1970 or 2000, which
typically indicate firmware epoch-counter rollover bugs.
}
}
\examples{
\dontrun{
rec    <- read_acttrust("recordings/P001.txt")
issues <- check_consistency(rec)
issues
}
}
