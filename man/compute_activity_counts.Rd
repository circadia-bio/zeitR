% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw_accelerometry.R
\name{compute_activity_counts}
\alias{compute_activity_counts}
\title{Compute PIM, TAT, and ZCM activity counts from raw triaxial acceleration}
\usage{
compute_activity_counts(
  x,
  y,
  z,
  sampling_rate,
  epoch_sec = 60,
  filter_low = 0.5,
  filter_high = 2.7,
  zcm_threshold = 0.01,
  tat_threshold = 0.05,
  metrics = c("PIM", "TAT", "ZCM")
)
}
\arguments{
\item{x, y, z}{Numeric vectors of raw triaxial acceleration samples (same
units -- typically g -- and length, uniformly sampled at
\code{sampling_rate}).}

\item{sampling_rate}{\code{numeric(1)}. Sampling rate of \code{x}/\code{y}/\code{z} in Hz.}

\item{epoch_sec}{\code{numeric(1)}. Epoch length in seconds. Default \code{60}
(1-minute epochs, the ActTrust standard used elsewhere in zeitR).}

\item{filter_low, filter_high}{\code{numeric(1)}. Band-pass cutoffs in Hz,
passed to \code{\link[mrpheus:bandpass_filter]{mrpheus::bandpass_filter()}}. Default \code{0.5}/\code{2.7}
(ActTrust-style, per Batista et al. 2026); use \code{0.25}/\code{2.5} for
GT3X+-style processing instead.}

\item{zcm_threshold}{\code{numeric(1)}. Dead-band around zero (same units as
\code{x}/\code{y}/\code{z}) a filtered axis must clear to register a zero crossing.
Default \code{0.01}.}

\item{tat_threshold}{\code{numeric(1)}. Amplitude threshold (same units) the
filtered norm must exceed to count toward TAT. Default \code{0.05}.}

\item{metrics}{\code{character()}. Which metrics to compute. Default
\code{c("PIM", "TAT", "ZCM")} (all three).}
}
\value{
A tibble with one row per epoch: \code{epoch} (integer index, \code{1}-based)
plus one column per requested metric.
}
\description{
Reproduces the general wrist-actigraph processing chain -- per-axis
DC removal and band-pass filter, vector norm, epoch-level integration --
that converts raw acceleration samples into the epoch-level activity
metrics used throughout the rest of zeitR (e.g. \code{\link[=read_acttrust]{read_acttrust()}}'s
\code{activity}/\code{ZCMn} columns) and the wider PA-intensity literature (see
\code{\link[=pa_equations]{pa_equations()}}).
}
\section{What this is (and isn't)}{
Filtering reuses \code{\link[mrpheus:remove_dc]{mrpheus::remove_dc()}} and \code{\link[mrpheus:bandpass_filter]{mrpheus::bandpass_filter()}}
-- the same zero-phase Butterworth implementation already validated as
part of mrpheus's YASA-parity PSG pipeline -- rather than a new,
unvalidated filter written from scratch. This makes \strong{mrpheus a required
package for this function specifically} (a \code{Suggests} dependency of
zeitR, checked at runtime; the rest of zeitR does not need it).

Reusing a validated filter primitive does not make the whole function
validated, though: the PIM/TAT/ZCM epoch-aggregation logic below (what
happens \emph{after} filtering) is still an original implementation with
nothing to check it against -- no raw-to-counts converter exists to
compare it to, and Condor's/ActiGraph's exact onboard threshold constants
are proprietary and unpublished. Built from the general processing
description in Batista et al. (2026, PLoS ONE) and standard actigraphy
literature. Treat its output as a reasonable approximation of
device-computed counts, not a validated reproduction -- and prefer
device-computed counts (\code{\link[=read_acttrust]{read_acttrust()}}) over this function whenever
they're available.
}

\section{Processing steps}{
\enumerate{
\item Each of \code{x}, \code{y}, \code{z} has its DC offset removed
(\code{\link[mrpheus:remove_dc]{mrpheus::remove_dc()}}) -- important for the z-axis in particular,
which typically carries a ~1 g gravity offset -- then is independently
band-pass filtered with a zero-phase (\code{\link[mrpheus:bandpass_filter]{mrpheus::bandpass_filter()}})
Butterworth filter (\code{filter_low}-\code{filter_high} Hz).
\item A vector norm is computed per sample: \code{sqrt(x^2 + y^2 + z^2)} on the
\emph{filtered} axes, matching the order described for ActTrust in Batista
et al. (2026): filter first, then combine axes.
\item Samples are grouped into non-overlapping epochs of
\code{epoch_sec * sampling_rate} samples. Trailing samples that don't fill
a complete final epoch are dropped, with a warning.
\item Per epoch:
\itemize{
\item \strong{PIM} (proportional integration mode) -- sum of the absolute
filtered norm across the epoch.
\item \strong{TAT} (time above threshold) -- seconds within the epoch where the
absolute filtered norm exceeds \code{tat_threshold}.
\item \strong{ZCM} (zero crossing mode) -- number of sign changes beyond a
\code{zcm_threshold} dead-band, summed across the three filtered axes,
within the epoch.
}
}

\code{zcm_threshold} and \code{tat_threshold} have no published reference value --
they are exposed as tunable parameters rather than given a false-precision
default. The values shipped here are small, physically motivated starting
points (see Arguments), not calibrated constants.
}

\examples{
set.seed(1)
n  <- 25 * 60 * 5  # 5 minutes at 25 Hz
x  <- rnorm(n, sd = 0.05)
y  <- rnorm(n, sd = 0.05)
z  <- rnorm(n, sd = 0.05) + 1  # gravity on the z-axis

\dontrun{
compute_activity_counts(x, y, z, sampling_rate = 25, epoch_sec = 60)
}
}
\seealso{
\code{\link[=pa_equations]{pa_equations()}}, \code{\link[=estimate_ee]{estimate_ee()}}, \code{\link[=classify_pa_counts]{classify_pa_counts()}} to go
from PIM counts to METs and PA intensity bands; \code{\link[=read_acttrust]{read_acttrust()}} for
device-computed counts (preferred over this function when available);
\code{\link[mrpheus:bandpass_filter]{mrpheus::bandpass_filter()}}, \code{\link[mrpheus:remove_dc]{mrpheus::remove_dc()}} for the underlying
filter primitives.
}
