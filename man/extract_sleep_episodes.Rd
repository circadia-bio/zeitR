% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sleep_classify.R
\name{extract_sleep_episodes}
\alias{extract_sleep_episodes}
\title{Extract sleep episodes from a CSPD-scored epoch table}
\usage{
extract_sleep_episodes(data, wake_thresh = 60L)
}
\arguments{
\item{data}{A tibble as returned by \code{\link[=detect_sleep_crespo]{detect_sleep_crespo()}}, containing
columns \code{datetime}, \code{ZCMn}, and \code{state}.}

\item{wake_thresh}{\code{integer(1)}. Minimum wake-bout length (epochs) used
by \code{.nights_df()} to separate episode boundaries. Default \code{60L}.}
}
\value{
A tibble with one row per detected episode and columns \code{bts}
(POSIXct), \code{gts} (POSIXct), \code{tbt}, \code{tst}, \code{sol}, \code{soi}, \code{waso}
(minutes), \code{nw} (integer), \code{eff} (0-1), \code{nap} (logical, always \code{FALSE}
-- classification into main/secondary happens in
\code{\link[=classify_sleep_episodes]{classify_sleep_episodes()}}).
}
\description{
Converts the epoch-level \code{state} vector produced by \code{\link[=detect_sleep_crespo]{detect_sleep_crespo()}}
into a per-episode tibble analogous to the Python SleepPipeline's
\code{results.nights}. Contiguous on-wrist sleep periods are delimited by
\code{.nights_df()}; per-epoch wake/sleep within each period is scored by
\code{\link[=score_epochs_cole_kripke]{score_epochs_cole_kripke()}} to derive WASO, SOL, SOI, TST, EFF, and NW.
All time metrics are returned in \strong{minutes}.
}
\seealso{
\code{\link[=classify_sleep_episodes]{classify_sleep_episodes()}}, \code{\link[=run_pipeline_native]{run_pipeline_native()}}
}
