% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pa_intensity.R
\name{estimate_ee}
\alias{estimate_ee}
\title{Estimate energy expenditure (METs) from activity counts}
\usage{
estimate_ee(
  counts,
  device = c("ACTT", "GT3X+"),
  placement = c("hip", "wrist"),
  equation_set = "batista2026"
)
}
\arguments{
\item{counts}{Numeric vector of activity counts (counts/min, over the same
epoch length used by the source equation set -- 1-minute for
\code{"batista2026"}). Negative values are treated as \code{0} with a warning, since
the underlying square-root transform is undefined below zero.}

\item{device}{\code{character(1)}. \code{"ACTT"} (default) or \code{"GT3X+"}.}

\item{placement}{\code{character(1)}. \code{"hip"} (default) or \code{"wrist"}.}

\item{equation_set}{\code{character(1)}, passed to \code{\link[=pa_equations]{pa_equations()}}. Default
\code{"batista2026"}.}
}
\value{
Numeric vector of estimated METs, same length as \code{counts}.
}
\description{
Applies the published ActTrust(R)/GT3X+ regression equation
(\code{sqrt(MET) = b0 + b1 * sqrt(activity_counts)}) from
\code{\link[=pa_equations]{pa_equations()}} to convert raw activity counts into estimated metabolic
equivalents (METs).
}
\examples{
estimate_ee(c(0, 5000, 25000, 50000), device = "ACTT", placement = "hip")
}
\seealso{
\code{\link[=pa_equations]{pa_equations()}} for the underlying coefficients and their
limitations, \code{\link[=classify_pa_intensity]{classify_pa_intensity()}} to convert METs into intensity
bands.
}
