% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare.R
\name{prepare_actigraphy}
\alias{prepare_actigraphy}
\title{Prepare a raw actigraphy tibble for analysis}
\usage{
prepare_actigraphy(x)
}
\arguments{
\item{x}{A tibble as returned by \code{\link[=read_acttrust]{read_acttrust()}}, containing at minimum
\code{int_temp} and \code{ext_temp} columns.}
}
\value{
A tibble of the same dimensions as \code{x} with additional or updated
columns: \code{state}, \code{offwrist}, \code{sleep}, \code{int_temp_}, \code{ext_temp_}.
}
\description{
Transforms the output of \code{\link[=read_acttrust]{read_acttrust()}} (or any reader that returns a
compatible tibble) into the working data frame expected by all detection
functions. Specifically:
}
\details{
\enumerate{
\item Clamps \code{int_temp} and \code{ext_temp} to the physiological range [0, 42] °C.
\item Adds min-max scaled temperature columns \code{int_temp_} and \code{ext_temp_}
(range [0, 1]) for plotting.
\item Ensures \code{state}, \code{offwrist}, and \code{sleep} columns are present and
initialised to \code{0}.
}

The original tibble is never modified; a copy is returned.
}
\examples{
\dontrun{
rec  <- read_acttrust("recordings/P001.txt")
prep <- prepare_actigraphy(rec)
}
}
