% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_axivity.R
\name{read_axivity}
\alias{read_axivity}
\title{Read an Axivity AX3/AX6 .cwa file into a zeitR-standard epoch tibble}
\usage{
read_axivity(
  path,
  tz = "UTC",
  epoch_sec = 60,
  filter_low = 0.25,
  filter_high = 2.5,
  zcm_threshold = 0.01,
  tat_threshold = 0.05
)
}
\arguments{
\item{path}{\code{character(1)}. Path to a \code{.cwa}/AX6 file, forwarded to
\code{\link[axR:axivity_read_cwa]{axR::axivity_read_cwa()}}.}

\item{tz}{\code{character(1)}. Time zone the device's clock was set to.
Default \code{"UTC"}.}

\item{epoch_sec}{\code{numeric(1)}. Epoch length in seconds, forwarded to
\code{\link[=compute_activity_counts]{compute_activity_counts()}}. Default \code{60}, matching the rest of zeitR.}

\item{filter_low, filter_high}{\code{numeric(1)}. Band-pass cutoffs in Hz,
forwarded to \code{\link[=compute_activity_counts]{compute_activity_counts()}}. Default \code{0.25}/\code{2.5}
(GT3X+-style preset -- see Details; no validated Axivity-specific
preset exists).}

\item{zcm_threshold, tat_threshold}{\code{numeric(1)}. Forwarded to
\code{\link[=compute_activity_counts]{compute_activity_counts()}}. Defaults \code{0.01}/\code{0.05}.}
}
\value{
A tibble with one row per epoch and the same columns as
\code{\link[=read_acttrust]{read_acttrust()}} (\code{datetime}, \code{activity}, \code{int_temp}, \code{ext_temp},
\code{ZCMn}, \code{light}, \code{state}, \code{offwrist}, \code{sleep}), plus one extra column
not present there: \code{TAT} (time above threshold, seconds/epoch, from
\code{\link[=compute_activity_counts]{compute_activity_counts()}}). \code{activity} is PIM. \code{ext_temp} is always
\code{NA} -- Axivity devices have a single on-body temperature sensor, no
separate ambient sensor. The tibble carries a \code{"zeitr_axivity"} class
and a \code{metadata} attribute (a named list with \code{device_id},
\code{session_id}, \code{sample_rate}, \code{epoch_sec}, \code{filter_low}, \code{filter_high},
\code{cwa_metadata} (axR's raw device metadata string), \code{source_file}).
}
\description{
Bridges \code{\link[axR:axivity_read_cwa]{axR::axivity_read_cwa()}}'s raw per-sample output (triaxial
acceleration at the device's native sampling rate) into the same
epoch-level, 9-column shape \code{\link[=read_acttrust]{read_acttrust()}} produces -- so an Axivity
recording can flow through the rest of zeitR (\code{\link[=run_pipeline]{run_pipeline()}},
\code{\link[=compute_npcra]{compute_npcra()}}, \code{\link[=compute_sri]{compute_sri()}}, etc.) exactly like an ActTrust one.
}
\section{What this does (and doesn't) validate}{
The raw-to-counts conversion itself is \code{\link[=compute_activity_counts]{compute_activity_counts()}} --
already documented there as an unvalidated approximation of onboard
device processing (no reference converter exists to check it against).
Axivity devices additionally have \strong{no published or validated
filter/threshold preset} at all (unlike ActTrust and GT3X+, which at
least have a documented processing description to approximate). The
\code{filter_low}/\code{filter_high} defaults here (\code{0.25}/\code{2.5} Hz) are
\code{\link[=compute_activity_counts]{compute_activity_counts()}}'s GT3X+-style preset, reused because it's
the closer starting point of the two existing options for a
research-grade wrist accelerometer like the AX3 -- \strong{not} because it has
been checked against real Axivity output. Treat \code{activity}/\code{ZCMn} from
this function as a rough approximation only; validate against a
reference (e.g. GGIR) before relying on it for any published analysis.

\code{ZCMn} is \code{compute_activity_counts()}'s raw \code{ZCM} count with no
additional normalisation applied -- named \code{ZCMn} only for column-name
compatibility with \code{\link[=read_acttrust]{read_acttrust()}}'s CK-scoring input, not because a
normalisation step has actually been performed.
}

\section{Sampling rate}{
\code{axivity_read_cwa()} reports \code{sample_rate} per sample (block-level, as
stored in the \code{.cwa} file). This function takes the single most common
value across the whole recording and uses it for the entire conversion;
if any samples report a different rate (a genuine rate change mid
recording, or a corrupt block), a warning names the discrepancy but the
dominant rate is still used throughout. Epoch boundaries and grouping for
\code{light}/\code{int_temp} averaging are derived from this same dominant rate,
matching \code{\link[=compute_activity_counts]{compute_activity_counts()}}'s own epoch grouping exactly
(including which trailing samples get dropped).
}

\section{Time zone}{
\code{axivity_read_cwa()} tags \code{timestamp} as UTC by convention (the device's
own real-time clock, not a true UTC source) -- exactly like
\code{\link[=read_acttrust]{read_acttrust()}} does for ActTrust's \code{DATE/TIME} column. \code{tz} here
re-labels the same clock reading under the recording's actual local time
zone (via \code{\link[lubridate:force_tz]{lubridate::force_tz()}}; no shift in wall-clock value), rather
than converting it -- set it to the time zone the device's clock was
actually set to for correct circadian alignment downstream.
}

\examples{
\dontrun{
rec <- read_axivity("recordings/P001.cwa", tz = "America/Sao_Paulo")
rec
attr(rec, "metadata")
}
}
\seealso{
\code{\link[=read_actigraphy]{read_actigraphy()}} (\code{device = "axivity"}),
\code{\link[=compute_activity_counts]{compute_activity_counts()}} for the underlying raw-to-counts
conversion and its validation caveats, \code{\link[=read_acttrust]{read_acttrust()}} for the
column shape this matches.
}
