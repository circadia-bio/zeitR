% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_pipeline}
\alias{run_pipeline}
\title{Run the full actigraphy sleep analysis pipeline}
\usage{
run_pipeline(
  path,
  tz = "UTC",
  wake_thresh = 60L,
  gap_s = 120,
  offwrist_args = list(),
  sleep_args = list(),
  nap_args = list()
)
}
\arguments{
\item{path}{\code{character(1)}. Path to the ActTrust \code{.txt} file.}

\item{tz}{\code{character(1)}. Recording time zone. Passed to \code{\link[=read_acttrust]{read_acttrust()}}.
Default is \code{"UTC"}.}

\item{wake_thresh}{\code{integer(1)}. Minimum wake-bout length (epochs) used to
separate sleep periods in \code{\link[=compute_waso]{compute_waso()}}. Default is \code{60}.}

\item{gap_s}{\code{numeric(1)}. Gap threshold (seconds) for \code{\link[=check_consistency]{check_consistency()}}.
Default is \code{120}.}

\item{offwrist_args}{\code{list}. Additional arguments passed to
\code{\link[=detect_offwrist_bimodal]{detect_offwrist_bimodal()}}. Default is an empty list.}

\item{sleep_args}{\code{list}. Additional arguments passed to
\code{\link[=detect_sleep_crespo]{detect_sleep_crespo()}}. Default is an empty list.}

\item{nap_args}{\code{list}. Additional arguments passed to
\code{\link[=detect_naps_crespo]{detect_naps_crespo()}}. Default is an empty list.}
}
\value{
A \code{zeitr_result} S3 object — a named list with:
\describe{
\item{\code{subject_id}}{\code{character} — derived from the input filename stem.}
\item{\code{source_file}}{\code{character} — absolute path to the input file.}
\item{\code{data}}{\code{tibble} — final epoch-level data frame with all state
columns populated.}
\item{\code{nights}}{\code{tibble} — per-night sleep statistics.}
\item{\code{issues}}{\code{tibble} — timestamp consistency issues (0 rows if none).}
\item{\code{metadata}}{\code{list} — device and subject metadata from the file header.}
}
}
\description{
Orchestrates the complete pipeline for a single ActTrust recording:
}
\details{
\enumerate{
\item \strong{Read} — \code{\link[=read_acttrust]{read_acttrust()}}
\item \strong{Consistency check} — \code{\link[=check_consistency]{check_consistency()}}
\item \strong{Prepare} — \code{\link[=prepare_actigraphy]{prepare_actigraphy()}}
\item \strong{Off-wrist detection} — \code{\link[=detect_offwrist_bimodal]{detect_offwrist_bimodal()}}
\item \strong{Main sleep period detection} — \code{\link[=detect_sleep_crespo]{detect_sleep_crespo()}}
\item \strong{Nap detection} — \code{\link[=detect_naps_crespo]{detect_naps_crespo()}}
\item \strong{WASO + nightly statistics} — \code{\link[=compute_waso]{compute_waso()}}
}
}
\examples{
\dontrun{
result <- run_pipeline("recordings/P001.txt", tz = "America/Sao_Paulo")
result$nights
result$data
}
}
\seealso{
\code{\link[=run_pipeline_batch]{run_pipeline_batch()}} for processing a directory of files.
}
