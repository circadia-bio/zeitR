% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/waso.R
\name{compute_waso}
\alias{compute_waso}
\title{Compute WASO and nightly sleep statistics}
\usage{
compute_waso(x, wake_thresh = 60L, search_gap = FALSE)
}
\arguments{
\item{x}{A tibble as returned by \code{\link[=detect_naps_crespo]{detect_naps_crespo()}} (or
\code{\link[=detect_sleep_crespo]{detect_sleep_crespo()}} if nap detection is skipped), containing columns
\code{datetime}, \code{ZCMn}, and \code{state}.}

\item{wake_thresh}{\code{integer(1)}. Minimum duration in epochs of a wake bout
required to delimit a new sleep period boundary. Default is \code{60}.}

\item{search_gap}{\code{logical(1)}. If \code{TRUE}, allows a gap search between
consecutive sleep periods when identifying night boundaries. Default is
\code{FALSE}.}
}
\value{
A list with two elements:
\describe{
\item{\code{nights}}{A tibble with one row per detected night/nap and
columns \code{night}, \code{is_nap}, \code{bed_time}, \code{get_up_time}, \code{tbt}, \code{tst},
\code{waso}, \code{sol}, \code{soi}, \code{nw}, \code{eff}.}
\item{\code{data}}{The input tibble \code{x} with \code{state} and \code{sleep} updated
with epoch-level Cole-Kripke wake/sleep scores.}
}
}
\description{
Scores each epoch within detected sleep periods as wake or sleep using
\code{\link[=score_epochs_cole_kripke]{score_epochs_cole_kripke()}}, then computes per-night statistics.
}
\details{
The following metrics are computed for each detected night / nap:\tabular{ll}{
   Metric \tab Definition \cr
   \code{tbt} \tab Total Bed Time — epochs from bed time to get-up time \cr
   \code{tst} \tab Total Sleep Time — \verb{tbt − waso − sol − soi} \cr
   \code{waso} \tab Wake After Sleep Onset — wake epochs between sleep onset and final wake \cr
   \code{sol} \tab Sleep Onset Latency — epochs from bed time to first sleep epoch \cr
   \code{soi} \tab Sleep Offset Inertia — trailing wake epochs at end of sleep period \cr
   \code{nw} \tab Number of awakenings — count of wake-onset transitions \cr
   \code{eff} \tab Sleep efficiency — \code{tst / tbt} \cr
}
}
\examples{
\dontrun{
rec    <- read_acttrust("recordings/P001.txt")
prep   <- prepare_actigraphy(rec)
prep   <- detect_offwrist_bimodal(prep)
prep   <- detect_sleep_crespo(prep)
prep   <- detect_naps_crespo(prep)
result <- compute_waso(prep)
result$nights
}
}
\references{
Cole, R. J., Kripke, D. F., Gruen, W., Mullaney, D. J., & Gillin, J. C.
(1992). Automatic sleep/wake identification from wrist activity.
\emph{Sleep}, 15(5), 461–469. \doi{10.1093/sleep/15.5.461}
}
