% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lids.R
\name{lids_transform}
\alias{lids_transform}
\title{Apply the LIDS (Locomotor Inactivity During Sleep) transform}
\usage{
lids_transform(
  activity,
  epoch_min = 1,
  method = c("gaussian", "mva"),
  win_min = 30,
  sigma_min = 5
)
}
\arguments{
\item{activity}{Numeric vector of activity counts for a single sleep bout,
in chronological order at a constant epoch length.}

\item{epoch_min}{\code{numeric(1)}. Epoch duration in minutes. Default \code{1}.}

\item{method}{\code{character(1)}. \code{"gaussian"} (default) or \code{"mva"}.}

\item{win_min}{\code{numeric(1)}. Smoothing window width in minutes (full
width for \code{"mva"}; +/- 3 sigma width for \code{"gaussian"}). Default \code{30}.}

\item{sigma_min}{\code{numeric(1)}. Gaussian kernel standard deviation in
minutes, used only when \code{method = "gaussian"}. Default \code{5}
(Hammad et al. 2026); ignored for \code{"mva"}.}
}
\value{
Numeric vector of smoothed LIDS values, same length as \code{activity}.
}
\description{
Converts an activity signal into "inactivity" via the LIDS non-linear
transform, then smooths it -- the first step of the LIDS ultradian-rhythm
pipeline (Winnebeck et al. 2018; Hammad et al. 2026). LIDS is only ever
computed \emph{inside} an already-identified sleep bout; extract bouts first
via \code{\link[=compute_lids]{compute_lids()}} or \code{\link[=detect_lids_bouts]{detect_lids_bouts()}}.
}
\details{
\deqn{\text{LIDS}_i = \frac{100}{1+x_i}}

where \eqn{x_i} is the raw activity count at epoch \eqn{i}. A LIDS value of
100 means zero movement; it falls toward 0 as movement increases.

Two smoothing methods are available, both over a nominal 30-min window:
\itemize{
\item \code{"gaussian"} (default) -- Gaussian kernel, standard deviation
\code{sigma_min} (default 5 min), truncated at +/- 3 sigma (Hammad et al.
2026).
\item \code{"mva"} -- centered moving average (Winnebeck et al. 2018;
\code{pyActigraphy}'s default). Uses zeitR's border-replicated internal
\code{rolling_mean_cpp()}, which replicates the edge value rather than
shrinking the window near the bout boundary (pandas'
\code{min_periods=1} behaviour) -- a minor difference confined to the first/
last ~15 min of each bout.
}

Any \code{NA} in \code{activity} (e.g. a brief off-wrist gap inside an otherwise
valid bout) is linearly interpolated first -- \code{\link[=fit_lids]{fit_lids()}} cannot handle
missing values.
}
\examples{
set.seed(1)
activity <- pmax(0, 20 + 15 * sin(seq(0, 6 * pi, length.out = 360)) +
                    rnorm(360, sd = 5))
lids <- lids_transform(activity, method = "gaussian")
}
\references{
Winnebeck, E. C., Fischer, D., Leise, T., & Roenneberg, T. (2018). Dynamics
and Ultradian Structure of Human Sleep in Real Life. \emph{Current Biology},
28(1), 49-59.e5. \doi{10.1016/j.cub.2017.11.063}

Hammad, G., Schoch, S. F., Engelmann, M., Spock, Z., Kurth, S., & Winnebeck,
E. C. (2026). Charting infant sleep cycle development using actigraphy:
Longitudinal evidence for ultradian cycle lengthening within the first
year of life. \emph{SLEEP}.
}
\seealso{
\code{\link[=fit_lids]{fit_lids()}}, \code{\link[=detect_lids_bouts]{detect_lids_bouts()}}, \code{\link[=compute_lids]{compute_lids()}}
}
