% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_acttrust.R
\name{read_acttrust}
\alias{read_acttrust}
\title{Read a Condor Instruments ActTrust actigraphy file}
\usage{
read_acttrust(path, tz = "UTC", encoding = "latin1")
}
\arguments{
\item{path}{\code{character(1)} or \code{fs::path}. Path to the ActTrust \code{.txt} file.}

\item{tz}{\code{character(1)}. Time zone string passed to
\code{\link[lubridate:parse_date_time]{lubridate::parse_date_time()}}. Defaults to \code{"UTC"}. Set to the local
recording time zone for correct circadian alignment.}

\item{encoding}{\code{character(1)}. File encoding. Defaults to \code{"latin1"},
which matches Condor's default export encoding.}
}
\value{
A tibble with one row per epoch and the following columns:
\describe{
\item{\code{datetime}}{\code{POSIXct} — epoch timestamp.}
\item{\code{activity}}{\code{double} — PIM activity count.}
\item{\code{int_temp}}{\code{double} — internal (on-body) temperature, °C.}
\item{\code{ext_temp}}{\code{double} — external (ambient) temperature, °C.
\code{NA} if unavailable.}
\item{\code{ZCMn}}{\code{double} — normalised zero-crossing mode count.
\code{NA} if unavailable.}
\item{\code{state}}{\code{double} — state column, initialised to \code{0}.}
\item{\code{offwrist}}{\code{double} — off-wrist indicator, initialised to \code{0}.}
\item{\code{sleep}}{\code{double} — sleep indicator, initialised to \code{0}.}
}
The tibble carries a \code{"zeitr_recording"} class and a \code{metadata} attribute
(a named list with \code{subject}, \code{device_id}, \code{device_model},
\code{firmware_version}, \code{interval_s}, \code{source_file}).
}
\description{
Parses a Condor ActTrust \code{.txt} export into a tidy tibble. The file
format consists of a variable-length key-value header block followed by
semicolon-delimited epoch rows. The header ends at the line beginning with
\code{DATE/TIME}.
}
\examples{
\dontrun{
rec <- read_acttrust("recordings/P001.txt")
rec
attr(rec, "metadata")
}
}
