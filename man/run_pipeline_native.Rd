% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_pipeline_native}
\alias{run_pipeline_native}
\title{Run the Vallim native actigraphy sleep analysis pipeline}
\usage{
run_pipeline_native(
  path,
  tz = "UTC",
  gap_s = 120,
  params = acttrust_params(),
  offwrist_args = list(),
  sleep_args = list(),
  classify_args = list(),
  holidays = NULL,
  free_days = c("Saturday", "Sunday"),
  quiet = FALSE
)
}
\arguments{
\item{path}{\code{character(1)}. Path to the ActTrust \code{.txt} file.}

\item{tz}{\code{character(1)}. Recording time zone. Default \code{"UTC"}.}

\item{gap_s}{\code{numeric(1)}. Gap threshold (seconds) for \code{\link[=check_consistency]{check_consistency()}}.
Default \code{120}.}

\item{params}{Device parameter preset, as returned by \code{\link[=acttrust_params]{acttrust_params()}}.}

\item{offwrist_args}{\code{list}. Additional arguments for \code{\link[=detect_offwrist_bimodal]{detect_offwrist_bimodal()}}.}

\item{sleep_args}{\code{list}. Additional arguments for \code{\link[=detect_sleep_crespo]{detect_sleep_crespo()}}.}

\item{classify_args}{\code{list}. Additional arguments for \code{\link[=classify_sleep_episodes]{classify_sleep_episodes()}}.}

\item{holidays}{Holidays to treat as free days in addition to the days in
\code{free_days}. Accepts three forms, which can be mixed in the same vector:
\itemize{
\item \code{Date} objects or \code{"YYYY-MM-DD"} strings for year-specific dates (e.g.
\code{as.Date("2019-03-04")} for one Carnival day).
\item \code{"DD-MM"} strings for dates that recur every year (e.g. \code{"25-12"} for
Christmas, \code{"07-09"} for Brazilian Independence Day).
Stored in \code{result$holidays} and auto-forwarded by the \code{zeitr_result} S3
methods of \code{\link[=compute_sleep_metrics]{compute_sleep_metrics()}} and \code{\link[=compute_cpd_metrics]{compute_cpd_metrics()}}.
Default \code{NULL}.
}}

\item{free_days}{A character vector of day names (\code{"Monday"} through
\code{"Sunday"}, case-insensitive) or ISO integers (1 = Monday ... 7 = Sunday)
identifying which days of the week are unconditionally treated as free
days. Stored in \code{result$free_days} and auto-forwarded by the \code{zeitr_result}
S3 methods of \code{\link[=compute_sleep_metrics]{compute_sleep_metrics()}} and \code{\link[=compute_cpd_metrics]{compute_cpd_metrics()}}.
Default \code{c("Saturday", "Sunday")}.}

\item{quiet}{\code{logical(1)}. Suppress timestamp warnings. Default \code{FALSE}.}
}
\value{
A \code{zeitr_result} S3 object with the same structure as \code{\link[=run_pipeline]{run_pipeline()}},
except \code{nights} additionally contains:
\describe{
\item{\code{sleep_type}}{\code{character} -- \code{"main"} or \code{"secondary"}.}
\item{\code{bed_time}, \code{get_up_time}}{POSIXct -- from JRSV bts/gts convention
(last sleep epoch for get-up, not first wake epoch).}
}
The \code{is_nap} column is retained for backwards compatibility
(\code{is_nap == (sleep_type == "secondary")}).
}
\description{
Orchestrates the vendor-independent (Vallim) sleep analysis pipeline for a
single ActTrust recording. Shares the read / prepare / off-wrist / CSPD
epoch-scorer stages with \code{\link[=run_pipeline]{run_pipeline()}}, then replaces Condor's
\code{nights_df} classification with the rule set developed by Julia Ribeiro da
Silva Vallim and ported from her condor pipeline (Fixes 25, 26a/b/c, 27, 29).
}
\details{
Steps:
\enumerate{
\item \strong{Read} -- \code{\link[=read_acttrust]{read_acttrust()}}
\item \strong{Consistency check} -- \code{\link[=check_consistency]{check_consistency()}}
\item \strong{Prepare} -- \code{\link[=prepare_actigraphy]{prepare_actigraphy()}}
\item \strong{Off-wrist detection} -- \code{\link[=detect_offwrist_bimodal]{detect_offwrist_bimodal()}}
\item \strong{Epoch scoring} -- \code{\link[=detect_sleep_crespo]{detect_sleep_crespo()}} (CSPD; same scorer as \code{\link[=run_pipeline]{run_pipeline()}})
\item \strong{Episode extraction} -- \code{\link[=extract_sleep_episodes]{extract_sleep_episodes()}}
\item \strong{Episode classification} -- \code{\link[=classify_sleep_episodes]{classify_sleep_episodes()}} (JRSV rules)
\item \strong{Fine-grained state} -- \code{\link[=compute_waso]{compute_waso()}} (Cole-Kripke within periods, for \code{result$data})
}
}
\examples{
\dontrun{
result <- run_pipeline_native("recordings/P001.txt", tz = "America/Sao_Paulo")
result$nights
}
}
\seealso{
\code{\link[=run_pipeline]{run_pipeline()}} for the vendor (Condor) pipeline,
\code{\link[=extract_sleep_episodes]{extract_sleep_episodes()}}, \code{\link[=classify_sleep_episodes]{classify_sleep_episodes()}}
}
