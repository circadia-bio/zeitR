% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lids.R
\name{fit_lids}
\alias{fit_lids}
\title{Fit a sloped cosine to a LIDS profile, scanning candidate periods}
\usage{
fit_lids(lids, epoch_min = 1, period_range = c(30, 180), period_step = 2)
}
\arguments{
\item{lids}{Numeric vector of (smoothed) LIDS values for one sleep bout,
as returned by \code{\link[=lids_transform]{lids_transform()}}. Must not contain \code{NA}.}

\item{epoch_min}{\code{numeric(1)}. Epoch duration in minutes. Default \code{1}.}

\item{period_range}{\code{numeric(2)}. Candidate period bounds in minutes.
Default \code{c(30, 180)} (Hammad et al. 2026, tuned for infant/ultradian
cycles); use \code{c(60, 180)} with \code{period_step = 5} to match Winnebeck et
al. (2018)'s adult/adolescent scan.}

\item{period_step}{\code{numeric(1)}. Step size in minutes for the period scan.
Default \code{2}.}
}
\value{
A named list:
\describe{
\item{\code{period_min}}{Estimated cycle length (minutes) at peak MRI.}
\item{\code{amplitude}}{\eqn{\sqrt{\alpha^2+\beta^2}}.}
\item{\code{phase_rad}}{\eqn{-\mathrm{atan2}(\beta,\alpha)}, radians; \code{0} =
LIDS peak at bout start.}
\item{\code{offset}}{Inactivity level at bout start (\eqn{b}).}
\item{\code{slope_per_60min}}{Linear trend, rescaled to LIDS units per hour.}
\item{\code{pearson_r}}{Correlation between fitted and observed LIDS.}
\item{\code{p_value}}{Two-sided p-value for \code{pearson_r} (\code{\link[stats:cor.test]{stats::cor.test()}}).}
\item{\code{mri}}{Munich Rhythmicity Index at the selected period.}
}
}
\description{
For a fixed period \eqn{T}, the sloped-cosine model
\deqn{f(t) = \alpha\cos(2\pi t/T) + \beta\sin(2\pi t/T) + b + s\,t}
is linear in \eqn{(\alpha,\beta,b,s)}, so it is solved by ordinary least
squares rather than a non-linear optimiser (Hammad et al. 2026). Candidate
periods are scanned over \code{period_range} in steps of \code{period_step}, and for
each one the fit's Munich Rhythmicity Index
(\eqn{\text{MRI} = 2 \times \text{amplitude} \times r}, Winnebeck et al.
2018) is computed. The period with the highest MRI is returned as the
bout's estimated ultradian cycle length -- the same selection rule used by
\code{pyActigraphy.analysis.LIDS.lids_fit()}, generalised here to include the
linear slope term.
}
\examples{
set.seed(1)
t <- seq(0, 300, by = 1)
lids <- 85 + 15 * cos(2 * pi * t / 60) - 0.05 * t + rnorm(length(t), sd = 2)
fit_lids(lids)
}
\references{
Winnebeck, E. C., Fischer, D., Leise, T., & Roenneberg, T. (2018). Dynamics
and Ultradian Structure of Human Sleep in Real Life. \emph{Current Biology},
28(1), 49-59.e5. \doi{10.1016/j.cub.2017.11.063}

Hammad, G., Schoch, S. F., Engelmann, M., Spock, Z., Kurth, S., & Winnebeck,
E. C. (2026). Charting infant sleep cycle development using actigraphy.
\emph{SLEEP}.
}
\seealso{
\code{\link[=lids_transform]{lids_transform()}}, \code{\link[=compute_lids]{compute_lids()}}
}
