% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/offwrist.R
\name{detect_offwrist_bimodal}
\alias{detect_offwrist_bimodal}
\title{Off-wrist detection using the Condor bimodal activity/temperature model}
\usage{
detect_offwrist_bimodal(
  x,
  hws = 10L,
  activity_quantile = 0.15,
  min_norm_activity = 0.015,
  nbins = 100L,
  min_offwrist_length = 10L,
  min_temp_threshold = 0.35
)
}
\arguments{
\item{x}{A tibble as returned by \code{\link[=prepare_actigraphy]{prepare_actigraphy()}}, containing
columns \code{datetime}, \code{activity} (PIM), \code{int_temp}, \code{ext_temp}, \code{state},
and \code{offwrist}.}

\item{hws}{\code{integer(1)}. Half-window size (in epochs) for rolling feature
extraction. Default is \code{10} (matching the Python pipeline).}

\item{activity_quantile}{\code{numeric(1)}. Quantile used to define "low
activity". Default is \code{0.15}.}

\item{min_norm_activity}{\code{numeric(1)}. Minimum normalised activity
threshold below which the low-activity cutoff is clamped. Default is
\code{0.015}.}

\item{nbins}{\code{integer(1)}. Number of histogram bins used when fitting the
GMM threshold. Default is \code{100}.}

\item{min_offwrist_length}{\code{integer(1)}. Minimum number of consecutive
off-wrist epochs required to retain a detected period. Shorter runs are
discarded. Default is \code{10}.}

\item{min_temp_threshold}{\code{numeric(1)}. Minimum normalised temperature
threshold; the fitted GMM threshold is clamped to this value if it falls
below it. Default is \code{0.35}.}
}
\value{
The input tibble \code{x} with \code{state} and \code{offwrist} columns updated.
Off-wrist epochs have \code{state == 4} and \code{offwrist == 0.25}.
}
\description{
Detects periods where the actigraph was not worn using the bimodal
algorithm developed by Condor Instruments. The algorithm proceeds in
three stages:
}
\details{
\enumerate{
\item \strong{Feature extraction} — rolling median of PIM activity and rolling
signal/variance of internal temperature are computed over a symmetric
window of half-width \code{hws} epochs.
\item \strong{Bimodal temperature threshold} — among epochs with low activity
median (below the \code{activity_quantile} quantile of normalised activity),
a 2-component Gaussian Mixture Model is fitted to the normalised
temperature distribution. The threshold between the two components is
taken as the minimum of the GMM density between the two means (or the
minimum of the smoothed histogram if that is lower). Ashman's D is
computed as a bimodality quality metric.
\item \strong{Initial classification & refinement} — epochs simultaneously
exhibiting low activity median AND low temperature are marked as
off-wrist. Short spurious off-wrist runs shorter than
\code{min_offwrist_length} epochs are removed.
}

Off-wrist epochs are encoded as \code{state == 4} (matching the Python
pipeline convention). The \code{offwrist} column is set to \code{0.25} for
off-wrist epochs for actogram overlay plotting.
}
\examples{
\dontrun{
rec  <- read_acttrust("recordings/P001.txt")
prep <- prepare_actigraphy(rec)
prep <- detect_offwrist_bimodal(prep)
sum(prep$state == 4)  # number of off-wrist epochs
}
}
\references{
The bimodal off-wrist algorithm was developed by Julius A. P. P. de Paula
at Condor Instruments (2023). It is not published in peer-reviewed
literature but the source code is available in the circadiaBase pipeline
repository. The Ashman D statistic is described in:

Ashman, K. M., Bird, C. M., & Zepf, S. E. (1994). Detecting bimodality
in astronomical datasets. \emph{The Astronomical Journal}, 108, 2348.
\doi{10.1086/117248}
}
