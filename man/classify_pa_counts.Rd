% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pa_intensity.R
\name{classify_pa_counts}
\alias{classify_pa_counts}
\title{Classify physical activity intensity directly from activity counts}
\usage{
classify_pa_counts(
  counts,
  device = c("ACTT", "GT3X+"),
  placement = c("hip", "wrist"),
  equation_set = "batista2026"
)
}
\arguments{
\item{counts}{Numeric vector of activity counts (counts/min, over the same
epoch length used by the source equation set -- 1-minute for
\code{"batista2026"}). Negative values are treated as \code{0} with a warning, since
the underlying square-root transform is undefined below zero.}

\item{device}{\code{character(1)}. \code{"ACTT"} (default) or \code{"GT3X+"}.}

\item{placement}{\code{character(1)}. \code{"hip"} (default) or \code{"wrist"}.}

\item{equation_set}{\code{character(1)}, passed to \code{\link[=pa_equations]{pa_equations()}}. Default
\code{"batista2026"}.}
}
\value{
An ordered factor, same length as \code{counts} and same levels as
\code{\link[=classify_pa_intensity]{classify_pa_intensity()}}.
}
\description{
Convenience wrapper combining \code{\link[=estimate_ee]{estimate_ee()}} and \code{\link[=classify_pa_intensity]{classify_pa_intensity()}}
in one call: converts raw ActTrust(R)/GT3X+ activity counts straight to a PA
intensity factor via the published \code{"batista2026"} (or future) equation
set, without needing to handle the intermediate MET values.
}
\examples{
classify_pa_counts(c(0, 5000, 25000, 50000), device = "ACTT", placement = "hip")
}
\seealso{
\code{\link[=estimate_ee]{estimate_ee()}}, \code{\link[=classify_pa_intensity]{classify_pa_intensity()}}, \code{\link[=pa_equations]{pa_equations()}} for
the coefficients and their generalisability caveats.
}
