% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{acttrust_params}
\alias{acttrust_params}
\title{ActTrust device parameter preset}
\usage{
acttrust_params()
}
\value{
A named list with elements \code{offwrist}, \code{sleep}, \code{nap}, and \code{waso}.
}
\description{
Returns a named list of all device- and study-specific parameter values used
by the zeitR pipeline when processing ActTrust recordings. Passing this list
(or a modified copy) to \code{\link[=run_pipeline]{run_pipeline()}} via the \code{params} argument fully
controls which values each detector stage receives, without having to touch
the individual function calls.
}
\details{
The list is organised into four named sections that map directly to the four
pipeline stages:

\describe{
\item{\code{offwrist}}{Parameters for \code{\link[=detect_offwrist_bimodal]{detect_offwrist_bimodal()}}. These values
were validated against the Condor circadiaBase pipeline using ActTrust
hardware. The refinement stage (\code{.bimodal_refine_acttrust}) is
device-specific and currently only validated for ActTrust.}
\item{\code{sleep}}{Parameters for \code{\link[=detect_sleep_crespo]{detect_sleep_crespo()}}. \code{sleep_quantile}
(0.365) is the ActTrust CSPD wrapper value; the original Crespo (2012)
algorithm uses 1/3.}
\item{\code{nap}}{Parameters for \code{\link[=detect_naps_crespo]{detect_naps_crespo()}}, passed as the \code{params}
argument. Equivalent to \code{.cspd_nap_params()}.}
\item{\code{waso}}{Parameters for \code{\link[=compute_waso]{compute_waso()}}.}
}

To adapt the pipeline for a different device, copy the list, modify the
relevant values, and pass it to \code{\link[=run_pipeline]{run_pipeline()}}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{p <- acttrust_params()
p$sleep$sleep_quantile <- 1/3   # use the original Crespo threshold
result <- run_pipeline("recording.txt", params = p)
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=run_pipeline]{run_pipeline()}} for the pipeline entry point.
}
