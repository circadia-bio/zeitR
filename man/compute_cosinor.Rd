% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cosinor.R
\name{compute_cosinor}
\alias{compute_cosinor}
\title{Cosinor rhythmometry (Cornelissen 2014)}
\usage{
compute_cosinor(x, col = "activity", period_min = 1440)
}
\arguments{
\item{x}{A \code{zeitr_recording}/\code{zeitr_result}, or a data frame / tibble
with at least a \code{datetime} column and the column named in \code{col}.}

\item{col}{\code{character(1)}. Name of the signal column to fit (e.g.
\code{"activity"}, \code{"light"}, \code{"int_temp"}). Default \code{"activity"}.}

\item{period_min}{\code{numeric(1)}. Fixed period in minutes. Default \code{1440}
(24h), matching the reference notebook's locked-period usage.}
}
\value{
A tibble with columns \code{participant_id}, \code{acrophase_time}
(\code{"HH:MM"} string), \code{acrophase_time_neg} (numeric, \verb{[-12, 12]}),
\code{MESOR}, \code{amplitude}, \code{period_min}, \code{n_epochs}.
}
\description{
Fits a single-harmonic cosine model to an activity/light/temperature
signal with a FIXED period (default 24h), returning the acrophase (peak
time), MESOR (rhythm-adjusted mean), and amplitude.
}
\details{
Ports pyActigraphy's actual \code{Cosinor} class (\code{pyActigraphy/analysis/ cosinor.py}) as it is really used in the reference notebook (Cell 16 of
\code{vs_condor_py_pipeline_fix30_jrsv.ipynb}'s \verb{_fit_cosinor()}), not
reconstructed from documentation. The model is
\deqn{y = M + A \cos(\omega x + \phi)}
with \eqn{\omega = 2\pi / T}, \eqn{x} the integer epoch position
(\verb{0, 1, 2, ...}, matching \code{Cosinor._convert_timestamp_to_index()}'s
\code{(index - index[0]) / index.freq}, not clock time or elapsed seconds),
\eqn{M} the MESOR, \eqn{A} the amplitude, \eqn{\phi} the acrophase.

Cell 16's wrapper explicitly locks \code{Period} (\code{vary=False}) rather than
fitting it -- its own comment explains why: leaving \code{Period} free let
the optimizer converge anywhere from 0.23h to 91.75h, making the
acrophase/period meaningless. With the period fixed, the model is
linear in disguise: expanding
\eqn{A\cos(\omega x+\phi) = A\cos\phi\cos(\omega x) - A\sin\phi\sin(\omega x)}
and substituting \eqn{\beta_1 = A\cos\phi}, \eqn{\beta_2 = -A\sin\phi}
gives \eqn{y = M + \beta_1\cos(\omega x) + \beta_2\sin(\omega x)}, an
ordinary linear regression in \eqn{(M, \beta_1, \beta_2)} -- solved here
via \code{qr.solve()}, not a nonlinear optimizer. Verified by direct
execution that this closed-form OLS solution matches pyActigraphy's
actual \code{lmfit} (Levenberg-Marquardt) fit to within floating-point
precision when the period is locked, which it always is in the
notebook's actual usage -- this is not an approximation.

\code{acrophase_time}/\code{acrophase_time_neg} reproduce the notebook's own
sign-flip fix exactly: the model's peak occurs at
\code{t_peak = -phi / omega = -phi * T / (2*pi)}, minutes since the first
epoch -- the notebook's own comment documents a prior bug where the
unnegated \code{+phi * T/(2*pi)} gave the ANTI-peak (trough), offset ~12h
from the true peak. \code{t_peak} is then wrapped to a 24h clock time via
\code{round(t_peak) \%\% 1440} -- hardcoded to 1440 regardless of the actual
\code{period_min} used for the fit, matching the source exactly (the
wrap-to-clock-time step assumes a 24h day, since it's meant to report a
wall-clock peak time, not a literal fit-period position).
\code{acrophase_time_neg} maps this to \verb{[-12, 12]} (values \verb{>= 12:00} shifted
by \code{-24}), matching the \verb{_om10_neg}/\verb{_ol5_neg} convention used elsewhere
in the same notebook for circular-safe comparison of clock times.
}
\examples{
\dontrun{
result <- run_pipeline_native("recordings/P001.txt", tz = "America/Sao_Paulo")
compute_cosinor(result)
compute_cosinor(result, col = "light")
}
}
\references{
Cornelissen, G. (2014). Cosinor-based rhythmometry. \emph{Theoretical
Biology and Medical Modelling}, 11(1), 16.
\doi{10.1186/1742-4682-11-16}
}
\seealso{
\code{\link[=compute_npcra]{compute_npcra()}}, \code{\link[=compute_sri]{compute_sri()}}
}
