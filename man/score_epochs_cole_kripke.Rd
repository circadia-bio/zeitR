% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cole_kripke.R
\name{score_epochs_cole_kripke}
\alias{score_epochs_cole_kripke}
\title{Score actigraphy epochs as wake or sleep using the Cole-Kripke algorithm}
\usage{
score_epochs_cole_kripke(
  zcm,
  P = 0.000464,
  weights_before = c(34.5, 133, 529, 375, 408, 400.5, 1074, 2048.5, 2424.5),
  weights_after = c(1920, 149.5, 257.5, 125, 111.5, 120, 69, 40.5)
)
}
\arguments{
\item{zcm}{\code{numeric} vector of ZCM activity counts, one value per epoch.}

\item{P}{\code{numeric(1)}. Scaling factor. Default is \code{0.000464} (Cole et al.,
1992).}

\item{weights_before}{\code{numeric(9)}. Weights applied to the 9 epochs
\emph{before} the current epoch. Defaults to the values from Cole et al.
(1992), Table 2.}

\item{weights_after}{\code{numeric(8)}. Weights applied to the 8 epochs \emph{after}
the current epoch. Defaults to the values from Cole et al. (1992),
Table 2.}
}
\value{
An integer vector the same length as \code{zcm}, with \code{1} indicating
wake and \code{0} indicating sleep.
}
\description{
Applies the Cole-Kripke algorithm to a vector of zero-crossing mode (ZCM)
activity counts, scoring each epoch as wake (\code{1}) or sleep (\code{0}) using a
weighted sum of activity in a surrounding window.
}
\details{
Each epoch's score is computed as:

\deqn{D_i = P \sum_{j=1}^{9} W_j^{-} \cdot A_{i-j} +
            P \sum_{j=1}^{8} W_j^{+} \cdot A_{i+j}}

where \eqn{A_i} is the ZCM count at epoch \eqn{i}, \eqn{W^{-}} and
\eqn{W^{+}} are the before and after weight vectors from Cole et al. (1992),
and \eqn{P = 0.000464}. Epochs with \eqn{D_i \ge 1} are scored as wake.
}
\examples{
\dontrun{
rec    <- read_acttrust("recordings/P001.txt")
scores <- score_epochs_cole_kripke(rec$ZCMn)
table(scores)  # 0 = sleep, 1 = wake
}
}
\references{
Cole, R. J., Kripke, D. F., Gruen, W., Mullaney, D. J., & Gillin, J. C.
(1992). Automatic sleep/wake identification from wrist activity.
\emph{Sleep}, 15(5), 461–469. \doi{10.1093/sleep/15.5.461}
}
