% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{export_hypnogram}
\alias{export_hypnogram}
\title{Export a zeitR pipeline result as a hypnoR-compatible hypnogram}
\usage{
export_hypnogram(
  result,
  subject_id = NULL,
  source = "zeitR",
  drop_offwrist = FALSE,
  epoch_sec = NULL
)
}
\arguments{
\item{result}{A \code{zeitr_result} list as returned by \code{\link[=run_pipeline]{run_pipeline()}} or
\code{\link[=run_pipeline_native]{run_pipeline_native()}}, or a tibble with at minimum the columns
\code{datetime} and \code{state}.}

\item{subject_id}{\code{character(1)} or \code{NULL}. Written to the \code{subject_id}
column and always takes precedence. When \code{NULL} (default),
\code{result$subject_id} is used if present; the column is omitted entirely
when no ID is available from either source.}

\item{source}{\code{character(1)}. Label written to the \code{source} column.
Default \code{"zeitR"}.}

\item{drop_offwrist}{\code{logical(1)}. Remove off-wrist epochs
(\code{state == 4}) from the output and re-index epochs. Default \code{FALSE}.}

\item{epoch_sec}{\code{numeric(1)}. When supplied, warns if the observed epoch
duration differs from this value. Default \code{NULL}.}
}
\value{
A tibble with columns:
\describe{
\item{\code{epoch}}{Integer epoch index, 1-based.}
\item{\code{time}}{\code{POSIXct} timestamp for the start of each epoch.}
\item{\code{stage}}{Ordered factor: \code{c("W", "Sleep", "Quiet sleep")}.}
\item{\code{subject_id}}{Character subject identifier (omitted when not
available).}
\item{\code{source}}{Character scorer label.}
}
}
\description{
Converts the epoch-level \code{data} tibble from \code{\link[=run_pipeline]{run_pipeline()}} or
\code{\link[=run_pipeline_native]{run_pipeline_native()}} into the tidy hypnogram format expected by
\code{hypnoR} metric functions.
}
\details{
The coarse (3-state) stage mapping used by \code{zeitR} is:\tabular{lll}{
   \code{state} \tab \code{ZCMn} \tab Stage \cr
   \code{0} \tab any \tab \code{"W"} \cr
   \code{1}, \code{7} \tab \verb{> 0} \tab \code{"Sleep"} \cr
   \code{1}, \code{7} \tab \verb{== 0} \tab \code{"Quiet sleep"} \cr
   \code{4} \tab any \tab \code{"W"} \cr
}


Zero-count epochs within sleep (\code{ZCMn == 0}) are mapped to \code{"Quiet sleep"}
as the standard actigraphy proxy for quiet/deep sleep. When \code{ZCMn} is not
present in the data, all sleep epochs are mapped to \code{"Sleep"}.

The \code{stage} column is an ordered factor with levels
\code{c("W", "Sleep", "Quiet sleep")}, matching \code{hypnoR}'s coarse resolution
contract. \code{"Quiet sleep"} is not produced by actigraphy but the level is
present so downstream \code{hypnoR} functions do not throw factor-level errors.

\code{subject_id} is taken from \code{result$subject_id} when not explicitly
supplied. Both \code{\link[=run_pipeline]{run_pipeline()}} and \code{\link[=run_pipeline_native]{run_pipeline_native()}} derive this
from the input filename stem automatically, so in most cases no manual
override is needed.
}
\examples{
\dontrun{
result <- run_pipeline("recordings/P001.txt", tz = "America/Sao_Paulo")

# subject_id inferred from filename automatically
hyp <- export_hypnogram(result)
hyp$subject_id  # "P001"

# Override when filename does not match study code
hyp <- export_hypnogram(result, subject_id = "STUDY_001")

# Batch: subject_id inferred for every file automatically
results <- run_pipeline_batch("recordings/", tz = "America/Sao_Paulo")
hyps    <- lapply(results, export_hypnogram)

# Drop off-wrist epochs
hyp_clean <- export_hypnogram(result, drop_offwrist = TRUE)
}
}
\seealso{
\code{\link[=run_pipeline]{run_pipeline()}}, \code{\link[=run_pipeline_native]{run_pipeline_native()}}, \code{\link[=label_states]{label_states()}}
}
