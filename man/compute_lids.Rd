% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lids.R
\name{compute_lids}
\alias{compute_lids}
\title{Compute LIDS ultradian-rhythm parameters for every sleep bout}
\usage{
compute_lids(
  x,
  bout_source = c("auto", "state", "roenneberg"),
  activity_col = "activity",
  method = c("gaussian", "mva"),
  win_min = 30,
  sigma_min = 5,
  period_range = c(30, 180),
  period_step = 2,
  duration_range = c(3, 12),
  min_r = 0.4,
  max_p = 0.05,
  offset_bounds = c(1, 99),
  bout_args = list()
)
}
\arguments{
\item{x}{A \code{zeitr_result}, \code{zeitr_recording}, or a data frame / tibble
with at least \code{datetime} and \code{activity} columns (and, for
\code{bout_source = "state"}/\code{"auto"}, a \code{state} column).}

\item{bout_source}{\code{character(1)}. \code{"auto"} (default), \code{"state"}, or
\code{"roenneberg"}. See Details.}

\item{activity_col}{\code{character(1)}. Name of the activity column in \code{x}.
Default \code{"activity"}.}

\item{method, win_min, sigma_min}{Forwarded to \code{\link[=lids_transform]{lids_transform()}}.}

\item{period_range, period_step}{Forwarded to \code{\link[=fit_lids]{fit_lids()}}.}

\item{duration_range}{\code{numeric(2)}, hours. Bout duration bounds (both
\code{bout_source} paths). Default \code{c(3, 12)}.}

\item{min_r, max_p, offset_bounds}{Quality-filter thresholds; see Details.}

\item{bout_args}{Named list of additional arguments forwarded to
\code{\link[=detect_lids_bouts]{detect_lids_bouts()}} when \code{bout_source = "roenneberg"} (e.g.
\code{relative_threshold}, \code{main_window}). Default \code{list()}.}
}
\value{
A tibble with one row per bout: \code{participant_id}, \code{bout_id},
\code{bout_start}, \code{bout_end}, \code{duration_h}, \code{period_min}, \code{amplitude},
\code{offset}, \code{slope_per_60min}, \code{phase_rad}, \code{pearson_r}, \code{p_value}, \code{mri},
\code{passes_quality_filter}.
}
\description{
The main entry point for the LIDS pipeline: extracts sleep bouts, applies
\code{\link[=lids_transform]{lids_transform()}} and \code{\link[=fit_lids]{fit_lids()}} to each, and returns one row per bout
with cosine-fit parameters and a quality-filter flag. Ports the full
pipeline described in Winnebeck et al. (2018) and Hammad et al. (2026).
}
\section{Where bouts come from (\code{bout_source})}{
\itemize{
\item \code{"state"} -- uses the epoch-level \code{state} column already produced by
zeitR's own pipelines (\code{\link[=run_pipeline]{run_pipeline()}} / \code{\link[=run_pipeline_native]{run_pipeline_native()}}):
contiguous \code{state == 1} runs are treated as bouts, filtered by
\code{duration_range}. Off-wrist (\code{state == 4}) epochs break a run rather
than being bridged.
\item \code{"roenneberg"} -- ignores any existing \code{state} column and runs the
independent \code{\link[=detect_lids_bouts]{detect_lids_bouts()}} relative-immobility detector directly
on the activity signal. Use this for standalone recordings that haven't
been run through zeitR's Crespo/Vallim pipelines, or to reproduce
Winnebeck/Hammad's own bout-detection method rather than zeitR's.
\item \code{"auto"} (the default) -- \code{"state"} if a \code{state} column is present in
\code{x}, otherwise \code{"roenneberg"}.
}
}

\section{Quality filtering}{
Following Winnebeck et al. (2018) and Hammad et al. (2026), a bout
\emph{passes} quality filtering when all of:
\itemize{
\item \code{pearson_r >= min_r} (default \code{0.4} -- a soft data-quality threshold,
not a hard significance test; ~75\% of adult bouts cleared this bar in
Winnebeck et al. 2018),
\item \code{p_value <= max_p} (default \code{0.05}),
\item \verb{offset_bounds[1] < offset < offset_bounds[2]} (default \verb{1 < offset < 99}), excluding spuriously flat bouts (e.g. a lost/removed device).
}

Bouts failing quality filtering are still returned (with
\code{passes_quality_filter = FALSE}) rather than dropped, so callers can
inspect what was excluded.
}

\examples{
\dontrun{
result <- run_pipeline_native("recordings/P001.txt", tz = "America/Sao_Paulo")
compute_lids(result)
}
}
\references{
Winnebeck, E. C., Fischer, D., Leise, T., & Roenneberg, T. (2018). Dynamics
and Ultradian Structure of Human Sleep in Real Life. \emph{Current Biology},
28(1), 49-59.e5. \doi{10.1016/j.cub.2017.11.063}

Hammad, G., Schoch, S. F., Engelmann, M., Spock, Z., Kurth, S., & Winnebeck,
E. C. (2026). Charting infant sleep cycle development using actigraphy:
Longitudinal evidence for ultradian cycle lengthening within the first
year of life. \emph{SLEEP}.
}
\seealso{
\code{\link[=lids_transform]{lids_transform()}}, \code{\link[=fit_lids]{fit_lids()}}, \code{\link[=detect_lids_bouts]{detect_lids_bouts()}},
\code{\link[=study_lids_metrics]{study_lids_metrics()}}
}
