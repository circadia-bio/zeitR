% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_actigraphy.R
\name{read_actigraphy}
\alias{read_actigraphy}
\title{Read an actigraphy file into a zeitr_recording object}
\usage{
read_actigraphy(path, device = "acttrust", tz = "UTC", ...)
}
\arguments{
\item{path}{\code{character(1)}. Path to the raw actigraphy file.}

\item{device}{\code{character(1)}. Device type. One of \code{"acttrust"} (default)
or \code{"axivity"}. Additional devices will be added in future versions.}

\item{tz}{\code{character(1)}. Recording time zone passed to the underlying
reader. Default is \code{"UTC"}.}

\item{...}{Additional arguments forwarded to the device-specific reader
(e.g. \code{encoding} for \code{\link[=read_acttrust]{read_acttrust()}}).}
}
\value{
A \code{zeitr_recording} S3 object — a named list with:
\describe{
\item{\verb{$epochs}}{A tibble with one row per epoch and columns
\code{datetime}, \code{activity}, \code{int_temp}, \code{ext_temp}, \code{ZCMn},
\code{state}, \code{offwrist}, \code{sleep}.}
\item{\verb{$metadata}}{A named list with \code{subject}, \code{device_id},
\code{device_model}, \code{firmware_version}, \code{interval_s}, \code{source_file},
and \code{participant_id} (derived from the filename stem).}
}
}
\description{
A device-agnostic wrapper that reads a raw actigraphy file and returns a
\code{zeitr_recording} object with \verb{$epochs} (a tidy tibble) and \verb{$metadata}
(a named list of device and recording information).
}
\details{
Currently supported devices:
\itemize{
\item \code{"acttrust"} — Condor Instruments ActTrust / ActTrust2 (\code{.txt})
\item \code{"axivity"} — Axivity AX3/AX6 (\code{.cwa}), via \code{\link[=read_axivity]{read_axivity()}}
(requires the \code{axR} package; converts raw acceleration to epoch-level
counts with \code{\link[=compute_activity_counts]{compute_activity_counts()}} -- see \code{\link[=read_axivity]{read_axivity()}}'s
Details for validation caveats)
}
}
\examples{
\dontrun{
rec <- read_actigraphy("recordings/P001.txt")
rec$epochs
rec$metadata
}
}
\seealso{
\code{\link[=read_actigraphy_dir]{read_actigraphy_dir()}} to read a whole directory at once.
}
