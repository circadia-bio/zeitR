% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lids.R
\name{study_lids_metrics}
\alias{study_lids_metrics}
\title{Batch LIDS ultradian-rhythm summary across a study}
\usage{
study_lids_metrics(results, min_bouts = 3, ...)
}
\arguments{
\item{results}{A named list of \code{zeitr_result} objects, as returned by
\code{\link[=run_pipeline_batch]{run_pipeline_batch()}} or \code{\link[=run_pipeline_native_batch]{run_pipeline_native_batch()}}.}

\item{min_bouts}{\code{integer(1)}. Minimum number of quality-filtered bouts
required for a participant's summary metrics to be reported (rather
than \code{NA}, with \code{n_bouts}/\code{n_bouts_passing} still populated). Default
\code{3}.}

\item{...}{Forwarded to \code{\link[=compute_lids]{compute_lids()}} for every participant.}
}
\value{
A tibble with one row per participant: \code{participant_id},
\code{n_bouts}, \code{n_bouts_passing}, and (computed only over
\code{passes_quality_filter == TRUE} bouts, and only when there are at
least \code{min_bouts} of them) \code{period_min_median}, \code{amplitude_median},
\code{offset_median}, \code{slope_per_60min_median}, plus an \verb{_iqr} variant of
each.
}
\description{
Computes \code{\link[=compute_lids]{compute_lids()}} for every participant in a batch of pipeline
results and summarises each participant's quality-filtered bouts into a
single row -- the LIDS counterpart to \code{\link[=study_sleep_metrics]{study_sleep_metrics()}} and
\code{\link[=study_summary]{study_summary()}}, making per-participant median cycle length, amplitude,
offset, and slope database-ready for tools like \code{syncR::sync()}.
}
\examples{
\dontrun{
results <- run_pipeline_native_batch("recordings/", tz = "America/Sao_Paulo")
study_lids_metrics(results)
}
}
\seealso{
\code{\link[=compute_lids]{compute_lids()}}, \code{\link[=study_sleep_metrics]{study_sleep_metrics()}}
}
