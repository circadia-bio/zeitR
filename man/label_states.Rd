% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{label_states}
\alias{label_states}
\title{Convert integer epoch states to a labelled factor}
\usage{
label_states(x)
}
\arguments{
\item{x}{integer (or numeric) vector of epoch states, as found in
\code{result$data$state}.}
}
\value{
An ordered factor with levels
\code{c("wake", "sleep", "nap", "off-wrist")}, the same length as \code{x}.
}
\description{
Converts the integer \code{state} column produced by the zeitR pipeline into a
human-readable factor. Useful for display, plotting, and export — the
internal \code{state} column always stays integer to preserve Python reference
parity.
}
\details{
\tabular{ll}{
   Integer \tab Label \cr
   \code{0} \tab \code{"wake"} \cr
   \code{1} \tab \code{"sleep"} \cr
   \code{4} \tab \code{"off-wrist"} \cr
   \code{7} \tab \code{"nap"} \cr
}


Any value not in the table above is silently converted to \code{NA}.
}
\examples{
label_states(c(0L, 1L, 0L, 4L, 1L, 7L))
# [1] wake  sleep wake  off-wrist sleep nap
# Levels: wake < sleep < nap < off-wrist

\dontrun{
result <- run_pipeline("recordings/P001.txt")
result$data$state_label <- label_states(result$data$state)
}
}
