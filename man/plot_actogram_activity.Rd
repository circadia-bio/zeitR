% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_actogram.R
\name{plot_actogram_activity}
\alias{plot_actogram_activity}
\title{Plot a double-plotted actogram with activity bars}
\usage{
plot_actogram_activity(
  result,
  tz = NULL,
  title = NULL,
  colours = NULL,
  activity_col = "ZCMn",
  activity_cap_quantile = 0.99,
  date_label_every = 7L,
  epoch_min = 1,
  base_size = 13
)
}
\arguments{
\item{result}{A \code{zeitr_result} list (from \code{\link[=run_pipeline]{run_pipeline()}} or
\code{\link[=run_pipeline_native]{run_pipeline_native()}}), or a tibble with at least \code{datetime} and
\code{state} columns.}

\item{tz}{\code{character(1)} or \code{NULL}. Timezone for date and time-of-day
extraction.  \code{NULL} (default) auto-detects from the timezone attribute
embedded in the \code{datetime} POSIXct column; falls back to \code{"UTC"} when
absent.}

\item{title}{\code{character(1)} or \code{NULL}. Plot title.  \code{NULL} constructs
\code{"Actogram \\u2014 <subject_id>"} from \code{result$subject_id} when available.}

\item{colours}{Named character vector mapping state labels (\code{"wake"},
\code{"sleep"}, \code{"nap"}, \code{"off-wrist"}) to hex colours.  \code{NULL} uses
\code{\link[=actogram_colours]{actogram_colours()}}.}

\item{activity_col}{\code{character(1)}. Name of the column in \code{result$data} to
use as the activity signal.  Default \code{"ZCMn"} (zero-crossing mean;
present in all ActTrust recordings processed by \code{zeitR}).}

\item{activity_cap_quantile}{\code{numeric(1)} in (0, 1]. Quantile of non-zero
activity values used to cap bar heights before normalising.  Default
\code{0.99} (top 1 \% of active epochs are clipped to full bar height).}

\item{date_label_every}{\code{integer(1)}. Label every Nth row on the y-axis.
Default \code{7} (weekly ticks).}

\item{epoch_min}{\code{numeric(1)}. Epoch duration in minutes.  Used as the tile
width in \code{\link[ggplot2:geom_tile]{ggplot2::geom_tile()}}.  Default \code{1} (ActTrust standard).}

\item{base_size}{\code{numeric(1)}. Base font size passed to
\code{\link[ggplot2:theme_minimal]{ggplot2::theme_minimal()}}.  Default \code{13}.}
}
\value{
A \code{ggplot} object.
}
\description{
Same double-plot layout as \code{\link[=plot_actogram_double]{plot_actogram_double()}} — each day appears in
both the left and right column of adjacent rows — but each epoch is rendered
as a vertical bar whose height is proportional to the raw activity count
(default: \code{ZCMn}, the zero-crossing mean from ActTrust).  Bars are coloured
by sleep/wake state, so the plot simultaneously conveys activity intensity
and state classification.
}
\details{
Bar heights are capped at the \code{activity_cap_quantile} quantile of
non-zero epochs to prevent outlier activity bursts from compressing the
visible range for the rest of the recording.  A thin baseline stub
(2 \% of row height) is drawn for zero-activity epochs so that sleep periods
and off-wrist blocks remain faintly visible.
}
\examples{
\dontrun{
result <- run_pipeline("recordings/P001.txt", tz = "America/Sao_Paulo")
plot_actogram_activity(result)

# Cap at 95th percentile to highlight moderate activity
plot_actogram_activity(result, activity_cap_quantile = 0.95)
}
}
\seealso{
\code{\link[=plot_actogram]{plot_actogram()}}, \code{\link[=plot_actogram_double]{plot_actogram_double()}}, \code{\link[=actogram_colours]{actogram_colours()}}
}
