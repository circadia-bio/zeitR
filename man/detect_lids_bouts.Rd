% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lids.R
\name{detect_lids_bouts}
\alias{detect_lids_bouts}
\title{Detect nighttime sleep bouts via the Roenneberg relative-immobility method}
\usage{
detect_lids_bouts(
  datetime,
  activity,
  relative_threshold = 0.15,
  ma_window_h = 24,
  bridge_min = 5,
  min_bout_min = 30,
  max_gap_min = 15,
  main_window = c("18:00", "08:00"),
  duration_range = c(3, 12),
  one_per_night = TRUE
)
}
\arguments{
\item{datetime}{\code{POSIXct} vector of epoch timestamps, regularly spaced.}

\item{activity}{Numeric vector of activity counts, same length as
\code{datetime}.}

\item{relative_threshold}{\code{numeric(1)}. Default \code{0.15}.}

\item{ma_window_h}{\code{numeric(1)}. Moving-average window in hours. Default
\code{24}.}

\item{bridge_min}{\code{numeric(1)}. Maximum active-blip length (minutes) to
bridge during consolidation. Default \code{5}.}

\item{min_bout_min}{\code{numeric(1)}. Minimum consolidated-run length
(minutes) to keep as a candidate bout. Default \code{30}.}

\item{max_gap_min}{\code{numeric(1)}. Maximum gap (minutes) between candidate
bouts to fuse them into one. Default \code{15}.}

\item{main_window}{\code{character(2)}. \code{c(start, end)} clock times
(\code{"HH:MM"}) defining the window a bout must \emph{start} within. Default
\code{c("18:00", "08:00")} (wraps midnight).}

\item{duration_range}{\code{numeric(2)}. Final bout duration bounds, in hours.
Default \code{c(3, 12)} (Winnebeck et al. 2018).}

\item{one_per_night}{\code{logical(1)}. If \code{TRUE} (default), keep only the
longest surviving bout per calendar night (the "main sleep episode").}
}
\value{
A tibble with one row per detected bout: \code{bout_id}, \code{bout_start},
\code{bout_end}, \code{duration_h}, \code{n_epochs}.
}
\description{
Standalone sleep-bout detector for raw actigraphy, independent of zeitR's
main Crespo-based pipeline (\code{\link[=detect_sleep_crespo]{detect_sleep_crespo()}}) or the Vallim native
pipeline (\code{\link[=run_pipeline_native]{run_pipeline_native()}}). Intended for LIDS analysis on activity
recordings that haven't been run through either of those. If you already
have a \code{zeitr_result}, pass it straight to \code{\link[=compute_lids]{compute_lids()}} instead
(\code{bout_source = "state"}, the default there) and skip this function.
}
\details{
Ported (with one bug fixed -- see below) from a prototype R notebook
translation of Mario Leocadio-Miguel's method, itself adapted from the
relative-immobility algorithm used in Winnebeck et al. (2018) and
Hammad et al. (2026):
\enumerate{
\item Compute a \code{ma_window_h}-centered moving average of activity (the
recording's own baseline).
\item Flag epochs where activity < \code{relative_threshold} times that
moving average as \emph{candidate sleep}.
\item \strong{Consolidate}: brief active blips (<= \code{bridge_min} minutes),
surrounded on both sides by candidate-sleep epochs, are relabelled as
sleep.
\item Keep only consolidated runs lasting >= \code{min_bout_min}.
\item \strong{Fuse} bouts separated by a gap <= \code{max_gap_min}. (This is the
step truncated mid-statement in the source notebook,
\code{fused.a...} -- reimplemented in full here as the internal
\code{.fuse_bouts()}. Unlike the notebook, which pads fused gaps with
\code{NaN} to preserve a regular time axis, the fused bout here simply
spans \code{start} to \code{end}
over the original activity values -- fine for bout \emph{timing}, but if
you need the gap epochs excluded from the LIDS fit itself, mask them
to \code{NA} in \code{activity} beforehand.)
\item Restrict to bouts starting within \code{main_window} (default
18:00-08:00), excluding daytime naps / recording artefacts.
\item Filter by total duration (\code{duration_range}) and, if
\code{one_per_night = TRUE}, keep only the longest bout per calendar night.
}

The moving-average step uses zeitR's border-replicated
\code{rolling_mean_cpp()} rather than pandas' \code{min_periods=1} edge behaviour
(which shrinks the window near the recording boundary instead of
replicating the edge value) -- a minor difference confined to the first/
last \code{ma_window_h}/2 hours of the recording.
}
\references{
Winnebeck, E. C., Fischer, D., Leise, T., & Roenneberg, T. (2018). Dynamics
and Ultradian Structure of Human Sleep in Real Life. \emph{Current Biology},
28(1), 49-59.e5. \doi{10.1016/j.cub.2017.11.063}
}
\seealso{
\code{\link[=compute_lids]{compute_lids()}}, \code{\link[=lids_transform]{lids_transform()}}, \code{\link[=fit_lids]{fit_lids()}}
}
